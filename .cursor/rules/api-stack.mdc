---
description: Stack et conventions API My-daily-page (NestJS, TypeORM, JWT, class-validator)
globs: api/**/*
alwaysApply: false
---

# API — Stack

Nestjs, TypeScript, PostgreSQL, TypeORM, JWT, class-validator + class-transformer

## Obligatoire
- Module + controller + service + `dto/`
- DTOs `class-validator` / `class-transformer`
- TypeORM `Repository`
- JWT sur toutes les routes sauf login / register

## Interdit
- Métier ou TypeORM dans le controller
- `userId` dans le body / query
- Resend / Twilio hors `notifications/providers/`
- Mot de passe en clair ou dans le JSON

```ts
@Injectable()
export class TasksService {
  constructor(@InjectRepository(Task) private readonly tasks: Repository<Task>) {}

  async findOne(id: string, userId: string) {
    const task = await this.tasks.findOne({ where: { id, userId } });
    if (!task) throw new NotFoundException(`Task "${id}" not found`);
    return task;
  }
}
```
